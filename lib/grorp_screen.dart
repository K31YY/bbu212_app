import 'package:flutter/material.dart';

class GrorpScreen extends StatelessWidget {
  const GrorpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Groups'),
      ),
      body: ListView(),
    );
  }
}