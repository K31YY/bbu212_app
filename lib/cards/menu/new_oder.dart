import 'package:flutter/material.dart';

class NewOder extends StatelessWidget {
  const NewOder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Order'),
      ),
      body: ListView(),
    );
  }
}